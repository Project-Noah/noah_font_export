Config = {}

Config.Fonts = {
     Serithai = {
          fontFile = "Serithai", -- the name of your .gfx, without .gfx
          FontId = "serithai", -- the name from the .xml
          Loaded = false -- Include this in every font, But don't change it's value
     }
}